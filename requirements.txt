# Kept in sync with pyproject.toml. If you change a version constraint in
# pyproject.toml, update the matching line here too — these two files
# previously drifted (requirements.txt had langgraph==0.2.74, a different
# major version than pyproject.toml's langgraph>=1.2.8), meaning `pip
# install -r requirements.txt` and `uv sync` could install genuinely
# different LangGraph APIs. Verified: langgraph==1.2.8 passes the full
# pytest suite (34/34) with no code changes needed.
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2

langgraph==1.2.8
langchain-core==1.4.8

mlflow==2.22.5

groq==0.13.1
python-dotenv==1.0.1
PyYAML==6.0.2

fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20

streamlit==1.41.1

joblib==1.4.2
pytest==8.3.4