# Copy this file to .env and fill in your own values.
# The app runs fine without GROQ_API_KEY too — the AI report and audit
# chat will automatically fall back to deterministic output instead of
# failing.

GROQ_API_KEY=your_groq_api_key_here
